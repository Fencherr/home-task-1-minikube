# MinIO Setup

## Bootstrap
MinIO credentials must be created manually (demo credentials):



Then apply the rest:
persistentvolumeclaim/minio-data unchanged
deployment.apps/minio configured
service/minio configured
