apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: qoves-api
  namespace: default-dev
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: qoves-api
  endpoints:
  - port: http
    path: /metrics
    interval: 15s