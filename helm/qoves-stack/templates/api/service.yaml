apiVersion: v1
kind: Service
metadata:
  name: qoves-api
  namespace: default-dev
  labels:
    app: qoves-api
spec:
  selector:
    app: qoves-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
